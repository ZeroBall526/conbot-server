package com.zeroball0526

import java.io.BufferedReader
import java.io.File
import java.io.FileReader

fun main(){
    /*
    var cnt = 0
    val file = File("${System.getProperty("user.dir")}\\conbot-image\\index.json")
    val reader = BufferedReader(FileReader(file, Charsets.UTF_8))
    reader.readLines().forEach{
        cnt++
        println("$cnt. $it")
    }

     */

    val a =imageIndexer("${System.getProperty("user.dir")}\\conbot-image")

}